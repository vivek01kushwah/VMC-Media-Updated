// This file is kept for legacy purposes only
// The actual app uses Next.js App Router in the /app directory
// See /app/page.tsx for the home page and /app/layout.tsx for the layout

// If using as a standalone React app (not recommended), uncomment below:
/*
import { createRoot } from "react-dom/client";
import "./index.css";
import App from "./App";

const rootElement = document.getElementById("root");
if (rootElement) {
  createRoot(rootElement).render(<App />);
}
*/
